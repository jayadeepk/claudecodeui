# Claude Code UI Environment Configuration
# Only includes variables that are actually used in the code

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (Express API + WebSocket server)
#API server
PORT=3001
#Frontend port
VITE_PORT=5173

# Base path for deployment (e.g., '/claude' for reverse proxy)
# Leave empty for root deployment
BASE_PATH=/claude

# =============================================================================
# VERSION CHECKING
# =============================================================================

# Disable version checking (shows "Update Available" notifications)
# Set to 'true' to disable version checks, 'false' or omit to enable (default)
VITE_DISABLE_VERSION_CHECK=true

# Hide Cursor CLI references in the UI (shows only Claude Code)
# Set to 'true' to hide Cursor mentions, 'false' or omit to show both CLIs (default)
VITE_DISABLE_CURSOR=true

# =============================================================================
# SESSION SUMMARY FILTERS
# =============================================================================

# Comma-separated list of prefixes to filter out from session summaries
# These messages will be excluded when generating session summaries
SESSION_SUMMARY_FILTER_PREFIXES=<command-name>,<command-message>,<local-command-,<bash-,</bash-,[Request interrupted by user
